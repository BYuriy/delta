alter table DELTA_CATEGORY add constraint FK_DELTA_CATEGORY_SUBGROUP_ID foreign key (SUBGROUP_ID) references DELTA_SUB_GROUP(ID);
create index IDX_DELTA_CATEGORY_SUBGROUP on DELTA_CATEGORY (SUBGROUP_ID);
